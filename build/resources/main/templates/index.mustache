{{>layout/header}}

<h1>이미지 갤러리</h1>
<div id="button-list">
{{^userName}}
    <a href="/oauth2/authorization/google" role="button" class="btn btn-outline-danger">Google Login</a>
{{/userName}}
{{#userName}}
    로그인 : {{userName}}
    <a href="/logout" role="button" class="btn btn-outline-danger">Logout</a>
    <label for="file" class="btn btn-outline-info">File Upload</label>
    <input id="file" type="file" name="uploadFile" enctype="multipart/form-data" accept=".gif, .jpg, .png">
    <input type="hidden" value="{{userName}}" id="userName">
{{/userName}}
    <a href="/log" role="button" class="btn btn-outline-warning">Login Log</a>
</div>

<div id="gallery">
{{#gallery}}
    <img src="/image/{{imageName}}" height="300" onclick="openChild({{id}})"/>
{{/gallery}}
</div>

{{>layout/footer}}